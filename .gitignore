# --- промежуточные результаты сборки ---
build/
sdk/build/
*.o
*.d
*.elf

# --- готовые образы: собираются командой make, а к релизу
#     прикладываются отдельным архивом, чтобы не раздувать историю ---
release/*.iso
release/*.img
release/*.bin
release/apps/*.kapp

# --- мусор редакторов ---
*~
.*.swp
.DS_Store

# Архив релиза
kvantos-*.tar.gz
